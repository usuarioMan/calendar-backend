const userLogin = (req, res) => {
  res.json({
    ok: true,
  });
};

const createNewUser = (req, res) => {
  res.json({
    ok: true,
  });
};

const refreshToken = (req, res) => {
  res.json({
    ok: true,
  });
};

module.exports = {
  userLogin,
  createNewUser,
  refreshToken,
};
